# API Documentation Guide

The API Generator catalogs services, endpoints, interfaces, and schemas.

---

## 1. Document Structure

The generated API guide details:
1. **Service Interfaces**: Lists methods, parameter details, and returns (e.g. `initialize()`, `start()`, `shutdown()`).
2. **Commands**: Console CLI endpoints related to the module.

---

## 2. CLI Invocation

```bash
# Generate API references for module 'workflow'
aios docs api workflow
```
